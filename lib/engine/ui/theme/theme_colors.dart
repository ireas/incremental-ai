import 'package:flutter/material.dart';

class ThemeColors {
  static Color backgroundPanel = HSLColor.fromAHSL(1, 0, 0, 0.05).toColor();
  static Color text = HSLColor.fromAHSL(1, 0, 0, 0.9).toColor();
  static Color textMuted = HSLColor.fromAHSL(1, 0, 0, 0.7).toColor();
}
