# Assembly

The **Assembly** is the central factory class where all module assemblers must be registered. It is used to call the assemblers assemble and
validation methods in order to provide a structured setup for all modules.